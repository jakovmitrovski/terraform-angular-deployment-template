profile    = "default"      # the client aws profile to use - has to be configured in ~/.aws/credentials - see https://docs.aws.amazon.com/cli/latest/userguide/cli-configure-profiles.html
aws_region = "eu-central-1" # the aws region to use
app_name   = "tftry"        # name of the app - has to be unique for creation to succeed